/*jslint node: true */
'use strict';

describe('DeleteModalInstanceController', function () {

    var scope,
        $rootScope,
        controller;

    beforeEach(function () {

        module('jbddApp');

        /*
         inject(function (_$rootScope_, _$q_, _$controller_) {
         $rootScope = _$rootScope_;
         scope = _$rootScope_.$new();
         controller = _$controller_('DeleteModalInstanceController', {
         '$scope': scope
         });

         });
         */

    });


    /*
     it('is awesome', function () {

     spyOn(controller.$modalInstance, 'close');
     spyOn(controller.$modalInstance, 'dismiss');

     var scope = $rootScope.$new();

     scope.ok();

     expect(controller.$modalInstance.close).toHaveBeenCalled();

     //expect($modalInstance.dismiss).toHaveBeenCalledWith('cancel');


     // $scope.entity = entity;

     });
     */

});
